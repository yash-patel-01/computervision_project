# Data directories (do not commit large datasets)
# Original raw datasets
data/jersey-2023/
data/tracking-2023/

# Generated YOLO datasets (symlinks/copies)
data/yolo_dataset_full_abs/
data/yolo_dataset_full_unique/
data/yolo_dataset_azure/

# Large COCO annotation files (too big for GitHub)
data/coco_train.json
data/coco_test.json

# Any nested image/label folders under data
data/**/images/
data/**/labels/

# Ultralytics/YOLO training artifacts (large files)
# Keep best.pt (6MB - critical trained weights), exclude other checkpoints
tracking_baseline/train/runs/yolo/**/weights/last.pt
tracking_baseline/train/runs/yolo/**/weights/epoch*.pt
tracking_baseline/train/runs/yolo/**/labels.jpg
tracking_baseline/train/runs/yolo/**/train_batch*.jpg
tracking_baseline/train/runs/yolo/**/val_batch*.jpg
tracking_baseline/train/runs/yolo/**/*.png

# Training logs (large)
*.log

# Local Ultralytics default project folder and artifacts
YOLOv8/
/runs/

# Downloaded pretrained weights (not our trained models)
yolov8n.pt
yolo11n.pt

# Keep small ball labeling file (4KB - will be committed)
# data/ball_tracks.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter Notebook
.ipynb_checkpoints
*/.ipynb_checkpoints/*

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Model checkpoints and training outputs
# Model checkpoint files (large binary files)
*.pth
# *.pt excluded above with specific exceptions for best.pt
*.h5
*.ckpt

# Keep training results and metrics (small JSON files will be committed)
# tracking_baseline/train/runs/frcnn/eval_metrics.json
# tracking_baseline/train/runs/frcnn/config.json
# tracking_baseline/train/runs/frcnn/heartbeat.json

# Keep training logs (will be committed)
# *.log

# Jupyter notebook (keep the notebook file, commit it)
# model_evaluation.ipynb
